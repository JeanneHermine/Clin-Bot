# Copy this file to .cloudflared/config.yml and replace the placeholders.
# The hostname must be a DNS record you control in Cloudflare.

tunnel: <TUNNEL-UUID>
credentials-file: /home/mev/.cloudflared/<TUNNEL-UUID>.json

ingress:
  - hostname: <your-stable-hostname>
    service: http://localhost:8000
  - service: http_status:404
